---
date: 2026-06-27
published: true
category: [Helix]
---

## 들어가며
최근 들어 Helix Editor를 사용하게 되어, Helix에서 자체적으로 제공하는 공식 문서인 Helix Tutor를 활용하여 Helix의 사용법을 정리해 보았습니다.
Helix를 처음 배우시는 분들께 많은 도움이 되길 바랍니다.

## 소개 (INTRODUCTION)

Helix 에디터에 오신 것을 환영합니다! Helix는 모달(modal) 방식이라는 점에서 여러분이 익숙한 다른 에디터들과 다릅니다. 즉, 텍스트 편집을 위한 여러 가지 모드를 가지고 있습니다. 주로 사용하게 될 모드는 **일반(Normal) 모드**와 **입력(Insert) 모드**입니다. 일반 모드에서는 키를 눌러도 실제로 텍스트가 입력되지 않습니다. 대신, 텍스트에 대한 다양한 작업을 수행하게 됩니다. 이를 통해 더 효율적인 편집이 가능합니다. 이 튜토리얼은 Helix의 모달 편집 기능을 활용하는 방법을 알려드릴 것입니다. 시작하려면 CapsLock 키가 눌려 있지 않은지 확인하고, 첫 번째 레슨에 도달할 때까지 `j` 키를 길게 누르세요.

> Normal 모드가 에디터의 중심 기반(기본 상태)이며, Insert 모드나 Select 모드 등은 특정 작업을 수행한 뒤 다시 일반 모드로 돌아오게 되는 보조적인 모드이다.
> 즉, Insert 모드에서 Select 모드를 가려고 해도 일단 Normal 모드를 거쳐야 한다.
> Normal 모드에서 Insert 모드든, Select 모드든 Normal이 아닌 모드로 어유일하게 들어 갈 수 있기 때문이다.

> Insert 모드나 Select 모드 등에서 Escape를 누르면 언제나 Normal 모드로 들어간다.

> 참고로, Helix의 모드는 다음과 같다.
> - 일반(Normal) 모드: 에디터를 켤 때의 기본 모드입니다. 텍스트 이동, 선택, 편집 명령을 내립니다.
> - 입력(Insert) 모드: i, a, o 등을 눌러 진입하며, 텍스트를 직접 입력합니다.
> - 선택(Select) 모드: 일반 모드에서 v를 눌러 진입합니다. 이 모드에서는 커서를 이동할 때마다 선택 영역이 계속 확장됩니다. (Vim의 Visual 모드와 유사합니다.)
> - 명령(Command) 모드: 일반 모드에서 :를 눌러 진입합니다. 파일 저장(:w), 종료(:q) 등 에디터 자체를 제어하는 명령을 입력합니다.
> - 매치(Match) 모드: 일반 모드에서 m을 눌러 진입합니다. 괄호 쌍 감싸기, 괄호 내부 선택 등의 작업을 수행합니다.

## 1.1 기본 커서 이동 (BASIC CURSOR MOVEMENT)



```
         ↑
         k        * h는 왼쪽에 있습니다.
     ← h   l →    * l은 오른쪽에 있습니다.
         j        * j는 아래 화살표처럼 생겼습니다.
         ↓
```

커서는 위에서 볼 수 있듯이 `h`, `j`, `k`, `l` 키를 사용하여 이동할 수 있습니다. 커서(방향) 키도 작동하지만, 앞으로 사용할 다른 키들과 가깝게 위치한 `hjkl` 키를 사용하는 것이 훨씬 빠릅니다. 이리저리 이동하며 `hjkl`에 익숙해져 보세요. 준비가 되면 `j`를 길게 눌러 다음 레슨으로 넘어가세요.

> 메모장이나 워드에서는 커서가 글자 사이에서 깜빡이는 '선(|)' 형태이지만, Helix에서는 글자 하나를 통째로 감싸는 '네모난 블록' 형태이다.
> Helix에서 이 커서 블록은 "현재 이 글자 1개가 선택되어 있다"는 것을 의미한다.
> 즉, 커서란 최소 1글자 크기를 가진 선택 영역이다.
> 단, Insert 모드에서 커서는 선 형태로 제공된다.
> 커서는 모든 모드에 항상 존재한다.
> 커서를 이동 시킬 수 있는 방법 중 하나는 `hjkl`이다.

> 커서 이동은 Insert 모드에서 방향키로도 할 수 있으나, 이제부터는 Normal 모드에서 `hjkl`로 해야 한다.
> - `h`는 왼쪽에 있으니 왼쪽(좌)으로 이동을 의미한다.
> - `l`는 오른쪽에 있으니 오른쪽(우)으로 이동을 의미한다.
> - `j`는 아래 화살표 처럼 생겼으니, 아래(하)로의 이동을 의미한다.
> - `k`는 위(상)로의 이동을 의미한다.

## 1.2 Helix 종료하기 (EXITING HELIX)

1. `:` 를 입력하여 명령(Command) 모드로 들어갑니다. 커서가 화면 맨 아래로 이동할 것입니다.
    
2. `q` 또는 `quit`를 입력하고 Enter를 눌러 Helix를 종료합니다.
    

참고: 저장되지 않은 변경 사항이 있으면 `quit` 명령이 실패합니다. 이러한 변경 사항을 무시하고 강제 종료하려면 `q!` 또는 `quit!`를 입력하세요. 파일을 저장하는 방법은 나중에 배울 것입니다.

명령을 입력하지 않고 명령 모드를 종료하려면 `Escape`를 누르세요. 이제 다음 레슨으로 넘어가세요.

> Command 모드는 Normal, Select 모드에서 `:`를 입력하면 들어갈 수 있다.

> Command 모드에서 `q`를 입력하고 Enter를 누르면 Helix 종료를 할 수 있으며 `w` 하지 않은 파일은 `q` 명령을 할 수 없다.
> `w` 하지 않은 파일에서 Helix를 종료하고 싶다면, `q!` 명령을 써야 한다.

## 1.3 삭제 (DELETION)

커서 아래에 있는 문자를 삭제하려면 `d` 키를 입력하세요.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. 여분의 문자로 커서를 이동한 후, `d`를 입력하여 삭제하세요.
    



```
--> Thhiss senttencee haass exxtra charracterss.
    This sentence has extra characters.
```

문장이 올바르게 수정되면 다음 레슨으로 넘어가세요.

> Helix에서 `d`는 단순히 글자를 지우는 키가 아니라, "현재 선택된 영역을 삭제하라"는 명령어이다.
> 따라서 커서가 글자 위에 올라가 있는 상태는 '그 글자 1개가 선택된 상태'이므로, d를 누르면 그 글자가 즉시 지워진다.
> 글자가 지워지면 그 오른쪽에 있던 글자들이 왼쪽으로 한 칸씩 당겨져 온다.
> `d` 명령어는 오직 Normal, Select 모드에서만 사용 가능하다.

## 1.4 입력 모드 (INSERT MODE)

`i` 키를 입력하여 입력(Insert) 모드로 들어갑니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. 텍스트가 누락된 곳으로 이동한 후 `i`를 눌러 입력 모드로 들어갑니다. 이제 입력하는 키가 텍스트로 입력됩니다.
    
3. 누락된 텍스트를 입력하세요.
    
4. `Escape`를 눌러 입력 모드를 종료하고 일반 모드로 돌아갑니다.
    
5. 아래 줄과 일치할 때까지 이 과정을 반복하세요.
    



```
--> Th stce misg so.
    This sentence is missing some text.
```

참고: 상태 표시줄에 현재 모드가 표시됩니다. `i`를 입력하면 'NOR'가 'INS'로 변경되는 것을 확인해 보세요.

> 오직 Normal, Select 모드에서 `i`를 누르면 Insert 모드로 들어갈 수 있다.
> Insert 모드에서 `esc`를 누르면 Normal 모드로 들어가게 된다.

## 1.5 파일 저장하기 (SAVING A FILE)

파일을 저장하려면 `:w` 또는 `:write`를 입력하세요.

1. 앞서 설명한 대로 `:q!`를 사용하여 Helix를 종료하거나, 새 터미널을 여세요.
    
2. `hx FILENAME`을 실행하여 Helix에서 파일을 엽니다.
    
3. 파일의 내용을 약간 편집합니다.
    
4. `:` 를 입력하여 명령 모드로 들어갑니다.
    
5. `w` 또는 `write`를 입력하고 Enter를 눌러 파일을 저장합니다.
    

`wq` 또는 `write-quit`를 입력하여 저장과 종료를 동시에 할 수도 있습니다.

참고: 원하는 경로에 저장하기 위해 `w` / `write` 명령 뒤에 선택적으로 파일 경로를 입력할 수 있습니다. 참고: 파일에 저장되지 않은 변경 사항이 있으면 상태 표시줄의 파일 이름 옆에 플러스 `[+]` 기호가 나타납니다.

## 1장 요약 (CHAPTER 1 RECAP)

- `h`,`j`,`k`,`l` 키를 사용하여 커서를 이동합니다.
    
- `:` 를 입력하여 명령 모드로 들어갑니다.
    
    - `q` / `quit` 및 `q!` / `quit!` 명령은 Helix를 종료합니다. 전자는 저장되지 않은 변경 사항이 있을 때 실패하며, 후자는 이를 무시하고 닫습니다.
        
    - `w` / `write` 명령은 파일을 저장합니다.
        
    - `wq` / `write-quit` 명령은 두 작업을 모두 수행합니다.
        
- 커서에 있는 문자를 삭제하려면 `d`를 입력합니다.
    
- `i`를 입력하여 입력 모드로 들어가 텍스트를 입력합니다. `Escape`를 눌러 일반 모드로 돌아갑니다.
    

## 2.1 추가적인 입력 명령 (MORE INSERT COMMANDS)

앞서 보았듯이 `i`를 입력하면 커서의 현재 위치에서 입력 모드로 들어갈 수 있습니다. 다른 위치에서 입력 모드로 들어갈 수 있는 몇 가지 방법이 더 있습니다.

일반적인 입력 명령의 예는 다음과 같습니다:

- `i` - 선택 영역의 **앞**에 입력합니다.
    
- `a` - 선택 영역의 **뒤**에 입력합니다. (a는 'append'를 의미합니다)
    
- `I` - 줄의 **시작** 부분에 입력합니다.
    
- `A` - 줄의 **끝** 부분에 입력합니다.
    

1. 아래 `-->`로 표시된 줄의 아무 곳으로나 커서를 이동하세요.
    
2. `A` (Shift-a)를 입력하면 커서가 줄의 끝으로 이동하고 입력할 수 있는 상태가 됩니다.
    
3. 아래 줄과 일치하도록 필요한 텍스트를 입력하세요.
    



```
--> This sentence is miss
    This sentence is missing some text.
```

## 2.2 새로운 줄 열기 (OPENING LINES)

`o`를 입력하면 새 줄을 추가하고 커서 **아래**에서 입력을 시작합니다. `O`를 입력하면 새 줄을 추가하고 커서 **위**에서 입력을 시작합니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. `o`를 입력하여 아래에 빈 줄을 열고 답변을 입력하세요.
    



```
--> What is the best editor?
```

## 2장 요약 (CHAPTER 2 RECAP)

- `a`를 입력하여 선택 영역 뒤에 내용을 덧붙입니다.
    
- `I`를 입력하면 줄의 맨 앞(공백을 제외한 첫 글자)에서 입력 모드로 들어갑니다.
    
- `A`를 입력하면 줄의 맨 끝에서 입력 모드로 들어갑니다.
    
- `o`와 `O`를 사용하여 각각 커서 아래와 위에 새 줄을 엽니다.
    

## 3.1 이동과 선택 (MOTIONS AND SELECTIONS)

`w`를 입력하면 다음 단어까지 앞쪽으로 선택합니다.

`d` 키는 실제로 커서에 있는 문자 하나만 삭제하는 것이 아니라, 선택된 모든 텍스트를 삭제합니다. 즉, 기본 상태의 커서는 '단일 문자가 선택된 상태'와 같습니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. 삭제해야 할 단어의 시작 부분으로 이동합니다.
    
3. `w`를 입력하여 다음 단어의 시작 부분까지 선택합니다.
    
4. `d`를 입력하여 선택 영역을 삭제합니다.
    
5. 줄에 있는 모든 불필요한 단어에 대해 이 과정을 반복하세요.
    



```
--> This sentence pencil has vacuum extra words in the it.
    This sentence has extra words in it.
```

## 3.2 추가적인 이동 명령 (MORE MOTIONS)

앞서 보았듯이 `w`를 입력하면 다음 단어의 시작점까지 앞쪽으로 이동하며 그 사이의 텍스트를 선택합니다. 이는 텍스트 안에서 이동하거나, 특정 텍스트를 선택하여 작업할 때 유용합니다.

일반적인 이동 명령은 다음과 같습니다:

- `w` - 다음 단어의 시작 지점 앞까지 이동합니다.
    
- `e` - 현재 단어의 끝까지 앞으로 이동합니다.
    
- `b` - 현재 단어의 시작 지점까지 뒤로 이동합니다.
    

커서 아래에 있는 단어를 선택하려면 `e`와 `b`를 조합해서 사용하세요.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. 'd' 글자로 이동합니다.
    
3. `e`를 입력하여 단어의 절반을 선택합니다.
    
4. `b`를 입력하여 나머지 절반을 선택합니다.
    



```
--> The Middle Kingdom.
```

## 3.3 단어(words)와 공백 단위 단어(WORDS) (WORDS AND words)

`w`, `e`, `b` 이동 명령은 각각 대문자인 `W`, `E`, `B`와 대응되며, 이는 일반적인 단어(words) 대신 공백 단위로 구분된 큰 단어(WORDS)를 탐색합니다. WORDS는 오직 공백(whitespace)으로만 구분되는 반면, words는 공백 외에도 다른 문자(기호 등)로 구분될 수 있습니다.

1. 아래 `-->`로 표시된 줄의 맨 앞으로 커서를 이동하세요.
    
2. 줄의 끝에 도달할 때까지 `w`를 반복해서 눌러 개별 단어들을 선택해 봅니다.
    
3. `one-of-a-kind`를 모두 지나가려면 7번의 키 입력이 필요하다는 점에 주목하세요. `"modal"`은 3번의 키 입력이 필요합니다.
    
4. 다시 `-->`로 표시된 줄의 처음으로 커서를 되돌리세요.
    
5. 이번에는 `W`를 반복해서 눌러 WORDS 단위로 선택해 봅니다.
    
6. `one-of-a-kind`와 `"modal"`이 모두 한 번의 키 입력만으로 선택되는 것을 확인하세요.
    



```
--> Helix is a one-of-a-kind "modal" text editor
```

## 3.4 변경 명령 (THE CHANGE COMMAND)

`c`를 입력하여 현재 선택 영역을 변경합니다.

변경(change) 명령은 현재 선택 영역을 삭제하고 바로 입력 모드로 들어가므로, `di` (삭제 후 삽입)의 매우 흔한 단축 방식입니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. 잘못된 단어의 시작 부분으로 이동한 후 `e`를 입력하여 단어를 선택하세요.
    
3. `c`를 입력하여 단어를 삭제함과 동시에 입력 모드로 들어갑니다.
    
4. 올바른 단어를 입력하세요.
    
5. 아래 줄과 완전히 같아질 때까지 반복하세요.
    



```
--> This paper has heavy words behind it.
    This sentence has incorrect words in it.
```

## 3.5 이동 명령과 횟수 지정 (COUNTS WITH MOTIONS)

이동 명령 앞에 숫자를 입력하면 해당 동작을 그 횟수만큼 반복합니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. `2w`를 입력하여 앞으로 2단어 이동합니다.
    
3. `3e`를 입력하여 3번째 단어의 끝부분으로 이동합니다.
    
4. `2b`를 입력하여 뒤로 2단어 이동합니다.
    
5. 다른 숫자들로 위의 과정들을 연습해 보세요.
    



```
--> This is just a line with words you can move around in.
```

## 3.6 선택 / 확장 모드 (SELECT / EXTEND MODE)

`v`를 입력하여 선택(Select) 모드로 들어갑니다. `v`를 다시 누르거나 `Escape`를 누르면 일반 모드로 돌아갑니다. 선택 모드에서는 기존 선택 영역을 대체하는 대신, 모든 이동 명령이 **선택 영역을 확장**시킵니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. FOO의 'F' 위치로 이동한 후 `v2w`를 입력하여 두 단어를 선택하세요.
    
3. `d`를 입력하여 두 단어를 삭제합니다. `d`를 누르면 일반 모드로 돌아온다는 것을 확인하세요.
    
4. BAZ의 'B' 위치로 이동하여, 같은 방법으로 두 단어를 지우세요.
    



```
--> Remove the FOO BAR distracting words BAZ BIZ from this line.
```

## 3.7 줄 선택하기 (SELECTING LINES)

`x`를 입력하여 줄 전체를 선택합니다. `x`를 다시 누르면 다음 줄까지 선택합니다.

1. 아래 두 번째 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. `x`를 입력하여 줄을 선택하고 `d`를 눌러 삭제하세요.
    
3. 네 번째 줄로 이동합니다.
    
4. `x`를 두 번 누르거나 `2x`를 입력하여 2줄을 선택하고, `d`를 눌러 삭제하세요.
    



```
--> 1) Roses are red,
--> 2) Mud is fun,
--> 3) Violets are blue,
--> 4) I have a car,
--> 5) Clocks tell time,
--> 6) Sugar is sweet,
--> 7) And so are you.
```

참고: 대문자 `X`도 `x`와 비슷하게 작동하지만 다음 줄로 선택 영역을 확장하지는 않습니다. 빈 줄에서 `X`를 누르면 아무 일도 일어나지 않습니다.

## 3.8 선택 영역 축소하기 (COLLAPSING SELECTIONS)

`;` 를 입력하여 선택 영역을 단일 커서 형태로 축소합니다.

때로는 커서를 이동하지 않고 선택을 해제하고 싶을 때가 있습니다. 이 작업은 `;` 키를 사용하여 할 수 있습니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. 지금까지 배운 이동 명령들을 사용하여 이리저리 이동해 보고, 선택된 상태에서 `;` 를 눌러 선택을 해제해 보세요.
    



```
--> This is an error-free line with words to move around in.
```

참고: 이는 선택 모드에서도 똑같이 작동합니다. 참고: 관련된 또 다른 명령어로 `Alt-;`가 있습니다. 이는 선택 방향을 뒤집습니다(선택 영역의 커서와 앵커의 위치를 서로 바꿈).

## 3장 요약 (CHAPTER 3 RECAP)

- `w`를 입력하면 다음 단어의 시작 부분까지 앞으로 선택합니다.
    
    - `e`를 입력하면 현재 단어의 끝까지 선택합니다.
        
    - `b`를 입력하면 현재 단어의 시작 부분으로 뒤로 선택합니다.
        
    - 대문자인 `W`, `E`, `B`를 사용하면 WORDS(공백 단위 단어)를 탐색합니다.
        
- `d`를 입력하여 전체 선택 영역을 삭제합니다.
    
    - `c`를 입력하면 선택 영역을 삭제하고 입력 모드로 들어갑니다.
        
- 이동 명령 앞에 숫자를 입력하면 그 횟수만큼 동작을 반복합니다.
    
- `v`를 입력하여 선택 모드로 들어가며, 이때 모든 이동 명령은 선택 영역을 확장합니다.
    
- `x`를 입력하여 현재 줄 전체를 선택합니다. `x`를 다시 입력하면 다음 줄까지 선택합니다.
    
- 세미콜론( `;` )을 입력하여 선택 영역을 축소(해제)합니다.
    

## 4.1 실행 취소하기 (UNDOING)

`u`를 입력하면 실행을 취소(undo)합니다. `U`를 입력하면 다시 실행(redo)합니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. 첫 번째 오류 위치로 이동한 후 `d`를 눌러 삭제하세요.
    
3. `u`를 입력하여 삭제한 것을 취소해 봅니다.
    
4. 해당 줄의 모든 오류를 수정하세요.
    
5. `u`를 여러 번 눌러 수정했던 내용을 되돌려 보세요.
    
6. `U` (Shift-u)를 여러 번 눌러 수정했던 상태로 다시 복구해 보세요.
    



```
--> Fiix the errors on thhis line and reeplace them witth undo.
```

## 4.2 텍스트 복사 및 붙여넣기 (COPYING AND PASTING TEXT)

`y`를 입력하여 선택 영역을 복사(yank)합니다. `p`를 입력하여 복사한 내용을 커서 **뒤**에 붙여넣기 합니다. `P`를 입력하여 복사한 내용을 커서 **앞**에 붙여넣기 합니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요. 커서가 "banana"의 'b' 위치에 있는지 확인하세요.
    
2. `w`를 입력하여 "banana"를 선택하고 `y`를 눌러 복사합니다.
    
3. "2"와 "3" 사이의 공백으로 이동하여 `p`를 눌러 붙여넣기 합니다.
    
4. "3"과 "4" 사이에서 똑같이 반복하세요.
    



```
--> 1 banana 2 3 4
    1 banana 2 banana 3 banana 4
```

참고: 텍스트를 삭제하거나 변경할 때마다 Helix는 변경된 텍스트를 복사합니다. 이를 방지하려면 대신 `Alt-d` / `Alt-c`를 사용하세요. 참고: Helix는 기본적으로 시스템 클립보드를 공유하지 않습니다. 시스템 클립보드에 복사/붙여넣기를 하려면 `Space` + `y` / `Space` + `p`를 사용하세요.

## 4.3 파일 내 검색하기 (SEARCHING IN FILE)

`/`를 입력하여 파일 내에서 앞으로 검색합니다. Enter를 눌러 검색을 확정합니다. `n`을 입력하면 다음 검색 결과로 이동합니다. `N`을 입력하면 이전 검색 결과로 이동합니다.

1. `/`를 입력한 뒤 'banana'와 같은 흔한 단어를 입력하세요.
    
2. Enter를 눌러 검색을 확정합니다.
    
3. `n`과 `N`을 사용하여 일치하는 결과들을 순환해 봅니다.
    

검색은 정규표현식(regular expressions)을 사용하므로 훨씬 더 복잡한 표현식을 타겟으로 삼을 수 있습니다. 이에 대해서는 선택(select) 명령 레슨에서 다룰 것입니다.

참고: 뒤로(역방향으로) 검색하려면 `?` (Shift-/)를 입력하세요. 참고: Vim과 달리 `?`는 탐색 방향을 바꾸지 않습니다. `N`은 항상 뒤로, `n`은 항상 앞으로 이동합니다.

## 4장 요약 (CHAPTER 4 RECAP)

- `u`를 입력하여 실행을 취소합니다. `U`를 입력하여 다시 실행합니다.
    
- `y`를 입력하여 텍스트를 복사(yank)하고, `p`를 입력하여 붙여넣습니다.
    
    - 시스템 클립보드를 사용하려면 `Space + y` 및 `Space + p`를 사용하세요.
        
- `/`를 입력하여 앞으로 검색하고, `?`를 입력하여 뒤로 검색합니다.
    
    - `n`과 `N`을 사용하여 검색 결과들을 순환합니다.
        

## 5.1 다중 커서 (MULTIPLE CURSORS)

`C`를 입력하여 조건에 맞는 다음 줄로 커서를 복제합니다.

1. 아래의 첫 번째 `-->` 줄로 커서를 이동하세요. 화살표(`-->`)를 지나서 커서를 두세요.
    
2. `C`를 입력하여 다음 줄로 커서를 복제하세요. 중간에 있는 빈 줄은 어떻게 건너뛰는지 확인하세요. 이제 키를 입력하면 두 커서에 동시에 적용됩니다.
    
3. 입력 모드를 사용하여 두 줄을 수정하세요. 두 개의 커서가 두 줄을 동시에 고칠 것입니다.
    
4. `,` 를 입력하여 첫 번째 커서를 제거하세요.
    



```
--> Fix th two nes at same ime.
-->
--> Fix th two nes at same ime.
    Fix these two lines at the same time.
```

참고: 커서 윗방향으로 똑같이 작동하게 하려면 `Alt-C`를 누르세요.

## 5.2 선택 명령 (THE SELECT COMMAND)

`s`를 입력하여 현재 선택 영역 내부에서 일치하는 것들을 찾아 선택합니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. `x`를 눌러 전체 줄을 선택합니다.
    
3. `s`를 입력합니다. 프롬프트가 나타날 것입니다.
    
4. `apples`를 입력하고 Enter를 누르세요. 줄에 있는 두 개의 'apples'가 모두 선택됩니다.
    
5. 이제 `c`를 누르고 'apples'를 'oranges' 같은 다른 단어로 변경하세요.
    
6. `Escape`를 눌러 입력 모드를 종료합니다.
    
7. `,` 를 입력하여 두 번째 커서를 제거하세요.
    



```
--> I like to eat apples since my favorite fruit is apples.
    I like to eat oranges since my favorite fruit is oranges.
```

## 5.3 정규표현식을 통한 선택 (SELECTING VIA REGEX)

검색과 마찬가지로, 선택(`s`) 명령도 정확한 문자열뿐만 아니라 정규표현식을 사용하여 선택할 수 있습니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. `x`를 눌러 줄을 선택한 다음 `s`를 입력합니다.
    
3. `' +'` (공백 두 개와 플러스 기호)를 입력하여 2개 이상의 연속된 공백을 모두 선택한 다음 Enter를 누르세요.
    
4. `c`를 입력하고 해당 공백들을 한 칸의 공백으로 변경하세요.
    



```
--> This  sentence has   some      extra spaces.
    This sentence has some extra spaces.
```

참고: '찾기 및 바꾸기(find-and-replace)'를 수행하고 싶다면 이 선택 명령이 해결책입니다. 교체하고 싶은 텍스트 영역을 선택한 후 (`%`를 누르면 파일 전체가 선택됩니다) 위에서 설명한 단계를 수행하세요.

## 5.4 선택 영역 정렬하기 (ALIGN SELECTIONS)

`&`를 입력하여 선택 영역의 내용을 정렬합니다.

1. 아래의 첫 번째 `-->` 줄로 커서를 이동하세요. 화살표 바로 뒤의 공백에 커서를 놓으세요.
    
2. `C`를 4번 누르거나 `4C`를 입력합니다.
    
3. `W`를 입력하여 숫자와 괄호를 선택합니다.
    
4. `&`를 입력하여 단어들을 나란히 정렬합니다.
    



```
--> 97) lorem
--> 98) ipsum
--> 99) dolor
--> 100) sit
--> 101) amet
```

참고: `&`는 선택 영역의 "머리(head)", 즉 이동하는 끝부분의 정렬만 고려합니다. 반대쪽 끝은 "앵커(anchor)"라고 부릅니다.

## 5.5 선택 영역을 줄 단위로 나누기 (SPLIT SELECTION INTO LINES)

`Alt-s`를 누르면 선택 영역을 개행문자(newline) 기준으로 분할합니다.

1. 아래 표의 첫 번째 줄로 커서를 이동하세요.
    
2. `6x`를 입력하여 전체 표를 선택하세요.
    
3. `Alt-s`를 눌러 각 줄마다 선택 영역이 나뉘게 합니다.
    
4. `&`를 눌러 표를 정렬하세요.
    



```
    | FRUIT   | AMOUNT |
    |---------|--------|
 | Apples  | 8      |
    | Bananas | 6      |
  | Oranges | 3      |
     | Donuts  | 4      |
```

## 5장 요약 (CHAPTER 5 RECAP)

- `C`를 입력하여 다음 줄로 커서를 복제하고, 이전 줄로 복제하려면 `Alt-C`를 사용합니다.
    
- `s`를 입력하여 현재 선택 영역 안에서 정규표현식 패턴과 일치하는 모든 항목을 선택합니다.
    
- `&`를 입력하여 다중 선택 영역들을 정렬합니다.
    
- `Alt-s`를 눌러 선택 영역을 각 줄별로 분할합니다.
    

## 6.1 특정 문자까지 선택하기 (SELECTING TO A CHARACTER)

`f<ch>`를 입력하면 해당 문자를 **포함하여** 그 문자까지 선택(find)합니다. `t<ch>`를 입력하면 앞과 같지만, 해당 문자를 **포함하지 않고** 직전까지만 선택(till)합니다. 대문자 `F` / `T`를 입력하면 반대 방향으로 똑같이 작동합니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요. 커서를 첫 번째 대시(`-`)에 위치시키세요.
    
2. `f[`를 입력하여 대괄호 `[` 까지 선택합니다.
    
3. `d`를 입력하여 선택 영역을 삭제합니다.
    
4. 줄의 끝으로 이동하여 `F]`를 입력해 똑같이 반복하세요.
    
5. `-->`로 표시된 두 번째 줄의 화살표 바로 뒤로 이동하세요.
    
6. `t`와 `T`를 사용하여 문장 양옆의 대시 기호들을 삭제해 보세요.
    



```
--> -----[Free this sentence of its brackets!]-----
--> ------Free this sentence of its dashes!------
```

참고: Vim과 달리 Helix는 이 명령을 현재 줄에만 국한시키지 않습니다. 파일 전체에서 해당 문자를 찾습니다.

## 6.2 교체 명령 (THE REPLACE COMMAND)

`r<ch>`를 입력하면 선택된 모든 문자를 `<ch>`로 교체(replace)합니다.

1. 아래 표의 두 번째 줄로 이동하여, 커서를 첫 번째 `=` 기호에 위치시키세요.
    
2. `t|` (Shift-)를 입력하여 `=` 구분선 부분만 선택합니다.
    
3. `r-`를 입력하여 구분선을 대시(`-`) 기호로 교체하세요.
    



```
| Month | Days |
|=======|------|
| Jan   | 31   |
| Feb   | 28   |
| Mar   | 31   |
| ...   | ...  |
```

## 6.3 반복하기 (REPETITION)

`.`를 입력하면 마지막으로 수행한 삽입/변경 명령을 반복합니다. `Alt-.`를 누르면 마지막으로 수행한 `f` / `t` 선택 명령을 반복합니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. 단어를 추가하거나 변경하는 등의 수정을 한 뒤, `.` 을 눌러 그 작업을 반복해 보세요.
    
3. 예를 들어 여러 문장을 선택하기 위해 `f`나 `t`를 사용한 뒤 `Alt-.` 를 사용해 보세요.
    



```
--> This is some text for you to repeat things. You can repeat
    insertions like changing words, or repeat selections like
    f / t.
```

## 6장 요약 (CHAPTER 6 RECAP)

- `f` / `F`를 입력하여 특정 문자를 포함할 때까지 선택 영역을 확장합니다.
    
    - `t` / `T`를 입력하여 특정 문자 직전까지 선택 영역을 확장합니다.
        
- `r`을 입력하여 선택된 문자를 다른 문자로 일괄 교체합니다.
    
- `.`를 입력하여 마지막 삽입 작업을 반복합니다.
    
    - `Alt-.`를 눌러 마지막 `f` / `t` 탐색 작업을 반복합니다.
        

## 7.1 복사한 텍스트로 교체하기 (REPLACE WITH YANKED TEXT)

`R`을 입력하면 현재 선택 영역을 이전에 복사한(yanked) 텍스트로 교체합니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. `w`를 입력하여 "watermelons"를 선택하고 `y`를 눌러 복사하세요.
    
3. `w`를 이용해 "oranges"를 선택하세요.
    
4. `R`을 입력하여 "oranges"를 "watermelons"로 교체하세요.
    



```
--> I like watermelons because oranges are refreshing.
    I like watermelons because watermelons are refreshing.
```

## 7.2 줄 합치기 (JOINING LINES)

`J`를 입력하면 선택 영역에 있는 여러 줄을 하나로 합칩니다(join).

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. `x`를 4번 누르거나 `4x`를 입력하여 4줄을 모두 선택하세요.
    
3. `J`를 입력하여 줄들을 하나로 합칩니다.
    



```
--> This sentence
is spilling over
onto other
lines.

    This sentence is spilling over onto other lines.
```

## 7.3 들여쓰기 (INDENTING LINES)

`>`를 입력하면 줄을 들여쓰기(indent) 하고, `<`를 입력하면 들여쓰기를 해제(unindent)합니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. 두 번째 줄로 내려가서 `>`를 입력해 들여쓰기 하세요.
    
3. 세 번째 줄로 이동하여 `<`를 입력해 들여쓰기를 해제하세요.
    



```
--> These lines
   are indented
         very poorly.

    These lines
    are indented
    much better.
```

## 7.4 숫자 증감시키기 (INCREMENTING AND DECREMENTING)

`Ctrl-a`를 누르면 선택 영역 내의 숫자가 증가합니다. `Ctrl-x`를 누르면 선택 영역 내의 숫자가 감소합니다.

1. 아래 `-->`로 표시된 세 번째 줄로 커서를 이동하세요.
    
2. 2로 표시된 두 번째 항목에서 `Ctrl-a`를 눌러 숫자를 3으로 증가시키세요.
    
3. 3으로 표시된 다음 항목에 대해서도 반복해 4로 만드세요.
    
4. 마지막 항목으로 이동한 뒤 `Ctrl-x`를 눌러 6을 5로 감소시키세요.
    



```
--> 1) First point.
--> 2) Added point.
--> 2) Next point.
--> 3) Another point.
--> 6) Last point.
```

## 7장 요약 (CHAPTER 7 RECAP)

- `R`을 입력하여 선택 영역을 복사해둔 텍스트로 교체합니다.
    
- `J`를 입력하여 선택된 줄들을 하나로 합칩니다.
    
- `>` 와 `<` 를 입력하여 들여쓰기를 추가하거나 제거합니다.
    
- `Ctrl-a`를 눌러 선택된 숫자를 증가시킵니다.
    
    - `Ctrl-x`를 눌러 선택된 숫자를 감소시킵니다.
        

## 8.1 레지스터 (REGISTERS)

레지스터는 복사한 텍스트 등을 저장하기 위해 하나의 문자로 식별되는 저장 공간입니다. 레지스터는 또한 가장 최근의 검색어를 저장하거나, 다음 섹션에서 배울 매크로를 저장하는 데에도 사용됩니다.

`"<ch>` 를 입력하면 레지스터 `<ch>`를 선택합니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. `w`를 입력하여 "watermelons"를 선택하고 `y`로 복사합니다.
    
3. `w`를 입력하여 "bananas"를 선택합니다.
    
4. `"b`를 입력하여 b 레지스터로 변경한 뒤 `y`를 눌러 복사합니다.
    
5. "mangoes"를 선택하고 `R`을 입력하여 "watermelons"로 교체하세요. (기본 레지스터 사용됨)
    
6. "pineapples"를 선택한 뒤 `"b R`을 입력하여 b 레지스터에 있는 "bananas"로 교체하세요.
    



```
--> I like watermelons and bananas because my favorite fruits
    are mangoes and pineapples.
```

## 8.2 매크로 (MACROS)

매크로는 반복하고자 하는 일련의 작업들을 기록하는 방법입니다. 특정 레지스터에 매크로를 기록할 수도 있습니다 (기본값은 `@` 레지스터). `Q`를 입력하여 매크로 기록을 시작합니다 - 화면 하단에 팝업이 나타날 것입니다. 기록을 중지하려면 다시 `Q`를 입력하세요. `q`를 입력하면 `@` 레지스터(기본값)에 저장된 매크로를 반복 실행합니다.

1. 아래 첫 번째 `-->` 줄로 커서를 이동하세요. 커서가 화살표의 `>`에 위치해야 합니다.
    
2. `Q`를 입력하여 매크로 기록을 시작합니다.
    
3. 맨 아래 줄과 일치하도록 줄을 수정합니다.
    
4. 입력 모드를 빠져나온 뒤 다시 `Q`를 입력하여 기록을 중지합니다.
    
5. 아래 줄로 이동하여 커서를 다시 `>` 위치에 놓습니다.
    
6. `q`를 입력하여 매크로를 실행하세요.
    



```
--> ... sentence doesn't have its first and last ... .
--> ... sentence doesn't have its first and last ... .
    This sentence doesn't have its first and last word.
```

## 8장 요약 (CHAPTER 8 RECAP)

- `"` 를 입력하여 다른 레지스터를 선택합니다.
    
- `Q`를 입력하여 레지스터에 매크로 기록을 시작하고 중지합니다 (기본값 `@`).
    
- `q`를 입력하여 `@` 또는 선택한 레지스터에서 매크로를 다시 실행합니다.
    

## 9.1 선택 영역으로 검색하기 (SEARCHING FOR SELECTIONS)

`/`를 이용한 가장 최근 검색어는 `/` 레지스터에 저장됩니다. `n`과 `N` 명령은 모두 `/` 레지스터를 참조하므로, 굳이 검색창을 띄우지 않아도 해당 레지스터의 값을 직접 설정할 수 있습니다.

`*` 를 입력하면 선택 영역의 내용을 `/` 레지스터로 복사하여, 검색어를 선택 영역의 텍스트로 바로 지정합니다. 이것은 기본 선택 영역(primary selection)을 복사하는 것으로, 선택 영역 순환 파트에서 더 배우게 됩니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. `e`를 사용하여 "horse"를 선택하고 `*`를 입력하세요.
    
3. `n`과 `N`을 사용하여 "horse" 사이를 점프해 봅니다.
    



```
--> A horse is a horse, of course, of course,
--> And no one can talk to a horse of course.
```

참고: `*`는 실제로 선택 영역을 `/` 레지스터로 복사할 뿐이므로 `"/y`의 단축키와 비슷합니다.

## 9.2 다음 검색 결과에 선택 영역 추가하기 (ADDING SELECTION ON NEXT SEARCH MATCH)

선택 모드(`v`) 상태에서 `n` 과 `N`을 사용할 때의 특징은, 선택 영역을 다음 검색 위치로 "이동"시키는 대신 검색 결과마다 "새로운 선택 영역을 추가"한다는 점입니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. 첫 번째 "bat"을 선택하고 `*`를 눌러 검색어로 설정하세요.
    
3. `v`를 눌러 선택 모드로 들어갑니다.
    
4. `n`을 눌러 다른 "bat"들도 모두 선택하세요.
    
5. `c`나 `r`을 사용하여 "bat"을 "cat"으로 변경하세요.
    



```
--> Everybody wants to be a bat,
--> because a cat's the only bat
--> who knows where it's at.
```

## 9.3 점프리스트 사용하기 (USING THE JUMPLIST)

Helix는 검색이나 코드의 함수 정의로 이동하는 등 큰 단위의 이동인 "점프(jumps)"를 추적할 수 있습니다. 이 기록을 점프리스트(jumplist)에 저장합니다.

`Ctrl-s`를 누르면 현재 위치를 점프리스트에 수동으로 저장합니다.

`Ctrl-i` ("in")와 `Ctrl-o` ("out")를 눌러 점프리스트 안에서 각각 앞으로, 뒤로 이동할 수 있습니다.

1. 어딘가에서 `Ctrl-s`를 누르세요.
    
2. 파일의 멀리 떨어진 곳으로 이동하세요.
    
3. `Ctrl-o`를 눌러서 (한 번만 누르세요!) 아까 저장했던 위치로 바로 되돌아오세요.
    

## 9.4 두 글자 라벨로 점프하기 (JUMP WITH TWO-CHARACTER LABELS)

`gw`를 입력하여 두 글자 라벨(labels) 기능을 활성화하세요. 각 단어의 시작 부분이 두 개의 강조된 문자로 대체됩니다. 나타난 두 문자를 차례로 입력하면 해당 라벨 위치로 즉시 점프하며, 취소하려면 `ESC`를 누르세요.

두 글자 라벨 기능을 사용하면 현재 화면에 보이는 어디로든 빠르게 이동할 수 있습니다.

1. 아래 `-->`로 표시된 줄의 맨 처음으로 커서를 이동하세요.
    
2. `gw`를 눌러 라벨 기능을 켠 다음, here의 맨 앞문자 he를 대체하고 있는 라벨 2글자를 입력하여 해당 단어로 단숨에 점프해 보세요.
    



```
--> This is just a simple line of text.
    There may be many such lines
    But you really want to jump here!
    This is fast with the 2-character labels.
```

## 9장 요약 (CHAPTER 9 RECAP)

- `*`를 입력하여 검색 레지스터를 기본 선택 영역의 내용으로 설정합니다.
    
- 선택 모드에서 `n` / `N`을 입력하여 검색 위치마다 다중 커서(선택 영역)를 추가합니다.
    
- `Ctrl-s`를 눌러 현재 위치를 점프리스트에 저장합니다.
    
    - `Ctrl-i`와 `Ctrl-o`를 눌러 점프리스트에서 앞/뒤로 이동합니다.
        
- `gw`를 입력하여 두 글자 라벨을 활성화하고 원하는 라벨 2글자를 입력해 즉시 점프하거나, `ESC`를 눌러 취소합니다.
    

## 10.1 선택 영역 순환 및 제거하기 (CYCLING AND REMOVING SELECTIONS)

`)` 와 `(` 를 누르면 여러 선택 영역들 사이에서 기본 선택 영역(primary selection) 포커스를 각각 앞으로, 뒤로 순환 이동시킵니다.

`Alt-,` 를 누르면 기본 선택 영역을 제거(해제)합니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. `xx` 또는 `2x`를 사용하여 두 줄을 모두 선택하세요.
    
3. `s`를 입력하여 선택 모드를 열고 "would"를 입력한 후 Enter를 누르세요.
    
4. `(` 와 `)` 를 사용하여 기본 선택 영역을 이리저리 이동해 보고, 두 번째 "would"에 포커스가 갔을 때 `Alt-,` 를 눌러 해당 선택을 제거하세요.
    
5. `c`를 입력하고 "wood"라고 쳐서 남은 "would"들을 "wood"로 변경하세요.
    



```
--> How much would would a wouldchuck chuck
--> if a wouldchuck could chuck would?
```

## 10.2 선택 영역의 내용 순환하기 (CYCLING THE CONTENT OF SELECTIONS)

`Alt-)` 와 `Alt-(` 를 누르면 다중 선택 영역들의 "내용물"을 각각 앞뒤로 서로 교체하며 순환시킵니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. `xx` 또는 `2x`를 사용하여 두 줄을 모두 선택하세요.
    
3. `s`를 입력하고 `"through|water|know"` 를 입력한 후 Enter를 누르세요.
    
4. `Alt-(` 와 `Alt-)` 를 사용하여 각 선택 영역의 내용이 서로 어떻게 순환되며 바뀌는지 확인해 보세요.
    



```
--> Jumping through the water,
--> daring to know.
```

## 10.3 대소문자 변경하기 (CHANGING CASE)

`~` 를 입력하면 선택된 글자의 대소문자를 서로 반전시킵니다. `` ` `` (백틱)을 입력하면 선택된 모든 글자를 소문자로 만듭니다. `` Alt-` `` 를 누르면 선택된 모든 글자를 대문자로 만듭니다.

1. 아래의 첫 번째 `-->` 줄로 커서를 이동하세요.
    
2. 잘못 대문자로 쓰이거나 소문자로 쓰인 문자를 각각 선택한 후 `~` 를 눌러 고치세요.
    
3. 두 번째 `-->` 줄로 이동하세요.
    
4. `x`를 눌러 줄을 선택하세요.
    
5. `` ` `` 를 입력하여 줄 전체를 소문자로 변경하세요.
    
6. 세 번째 `-->` 줄로 이동하세요.
    
7. `x`를 눌러 줄을 선택하세요.
    
8. `` Alt-` `` 를 입력하여 줄 전체를 대문자로 변경하세요.
    



```
--> thIs sENtencE hAs MIS-cApitalIsed leTTerS.
--> this SENTENCE SHOULD all be in LOWERCASE.
--> THIS sentence should ALL BE IN uppercase!
```

## 10.4 선택 영역 분할하기 (SPLITTING SELECTIONS)

`S`를 입력하면 정규표현식 패턴을 기준으로 각 선택 영역을 여러 개로 분할(split)합니다.

1. 아래 `---` 표시 밑의 줄로 커서를 이동하세요.
    
2. `xx` / `2x`를 눌러 두 줄을 선택하세요.
    
3. `S`를 누른 다음 `\. |!` 를 입력하고 Enter를 누르세요 (마침표 앞의 백슬래시, 마침표와 파이프 사이의 공백에 주의하세요). 이는 마침표나 느낌표를 기준으로 선택 영역을 문장 단위로 분할합니다.
    
4. `Alt-;` 를 눌러 선택 영역의 방향(앵커)을 뒤집으세요.
    
5. `;` 를 입력하여 선택 영역들을 한 글자 크기로 축소합니다 - 각 문장의 첫 글자만 남게 됩니다.
    
6. `` Alt-` `` 를 눌러 선택된 글자들을 대문자로 변환하세요.
    



```
---
these are sentences. some sentences don't start with uppercase
letters! that is not good grammar. you can fix this.
```

## 10장 요약 (CHAPTER 10 RECAP)

- `)` 와 `(` 를 사용하여 다중 커서 상황에서 기본 선택 영역 포커스를 각각 앞뒤로 이동합니다.
    
    - `Alt-,` 를 누르면 현재 포커스된 기본 선택 영역을 제거합니다.
        
    - `Alt-)` 와 `Alt-(` 를 누르면 선택된 텍스트들의 내용물 자체를 순환 교환합니다.
        
- `~` 를 입력하여 선택된 글자들의 대소문자를 반전시킵니다.
    
    - `` ` `` 와 `` Alt-` `` 를 사용하여 선택된 글자를 각각 소문자, 대문자로 변환합니다.
        
- `S`를 입력하여 정규표현식 매칭을 기준으로 선택 영역을 쪼갭니다.
    

## 11.1 한 줄 주석 처리하기 (COMMENTING A LINE)

`Ctrl-c`를 누르면 커서가 있는 줄을 주석 처리(comment)합니다. 주석을 해제하려면 `Ctrl-c`를 다시 누르세요.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. `-->`로 표시된 줄을 주석 처리해 보세요.
    
3. 이번에는 주석을 다시 해제해 보세요.
    



```
--> Comment me please
```

## 11.2 여러 줄 주석 처리하기 (COMMENTING MULTIPLE LINES)

선택 영역이나 다중 커서 기능을 사용하면, 선택 영역이나 커서가 위치한 모든 줄을 한꺼번에 주석 처리할 수 있습니다.

1. 아래 `-->`로 표시된 줄로 커서를 이동하세요.
    
2. `-->`로 표시된 다른 줄들을 전부 포함하도록 선택하거나 다중 커서를 만드세요.
    
3. 해당 줄들을 한 번에 주석 처리하세요.
    



```
--> How many are you going to comment?
--> Is this enough for a comment?
--> What are you doing?!
--> Stop commenting me!
--> AAAAaargh!!!
```

참고: 선택 영역이나 다중 커서 안에 이미 주석 처리된 줄이 포함되어 있다면, 그 줄은 주석이 해제되는 것이 아니라 주석 기호가 한 번 더 추가됩니다.

## 11장 요약 (CHAPTER 11 RECAP)

- `Ctrl-c`를 눌러 커서가 있는 줄을 주석 처리합니다. `Ctrl-c`를 다시 누르면 주석이 해제됩니다.
    
- 여러 줄을 주석 처리하려면 다중 커서나 선택을 통해 범위를 잡은 후 `Ctrl-c`를 누르세요.
    
    - 여러 줄이 선택된 상태에서 주석을 실행하면, 이미 주석 처리된 줄은 주석이 해제되지 않고 주석 기호가 이중으로 붙습니다.
        

## 12.1 매치 모드 점프 사용하기 (USING MATCH MODE JUMP)

일반 모드에서 매치 모드(Match mode)로 전환하려면 `m`을 입력하세요. 이 기능은 괄호 쌍과 그 안의 내용물을 다룰 때 특히 유용합니다.

도움말 팝업에서 볼 수 있듯이, 매치 모드에서는 여러 가지 동작을 수행할 수 있습니다. 짝을 이루는 괄호로 점프하려면 단순히 `mm`을 누르면 됩니다. 예를 들어 아래 줄(`-->`로 시작)에서 일반 모드 상태로 커서를 `(` 에 놓고 `mm`을 누르면 짝을 이루는 `)` 로 점프합니다. 아래 줄에서도 똑같이 할 수 있습니다: 예를 들어 `]` 로 이동한 뒤 `mm`을 누르면 `[` 로 점프합니다.



```
--> you can (jump between matching parentheses)
--> or between matching [ square brackets ]
--> now { you know the drill: this works with brackets too }
```

## 12.2 매치 모드 내부 선택 사용하기 (USING MATCH MODE SELECT INSIDE)

매치 모드를 사용하면 괄호나 다른 구분자 쌍 "내부(inside)"의 내용물만 선택할 수 있습니다. 아래 줄에서 확인해 보세요:

`-->` 줄로 이동하여, 일반 모드 상태로 괄호 사이의 아무 위치(예: 'x' 위치)에 커서를 놓고 `mi(` 또는 `mi)` 를 누르면 괄호 내부의 전체 내용물만 선택됩니다 (괄호 자체는 제외됨). 선택이 끝나면 평소처럼 원하는 작업(예: `c`를 눌러 변경)을 수행할 수 있습니다.



```
--> outside and (inside x parentheses) - and outside again
```

아래 문장들을 통해 `[]` 나 `{}`, 또는 이들이 중첩된 조합에서도 똑같이 작동하는지 테스트해 보세요 (항상 커서를 바로 감싸고 있는 가장 가까운 괄호 쌍을 기준으로 작동합니다). `""` 같은 기호에서도 동작합니다.



```
--> test [ with square brackets ] !
--> try ( with nested [ pairs of ( parentheses) and "brackets" ])
```

## 12.3 매치 모드 주변 선택 사용하기 (USING MATCH MODE SELECT AROUND)

내부 내용물뿐만 아니라 괄호(구분자) 자체까지 포함하여 "주변(around)"을 모두 선택하고 싶다면 `ma` 선택 명령을 사용합니다. 예를 들어 아래 줄로 이동하여 `()` 사이 아무 곳에 커서를 두고 `ma(` 또는 `ma)` 를 누르면, 둘러싼 `()` 까지 포함하여 내용 전체가 선택됩니다. 평소처럼 선택 영역에 대고 원하는 작업을 할 수 있으며, 예를 들어 `ma(d`를 누르면 통째로 삭제됩니다.



```
--> you ( select x around ) to include delimiters in the select
```

이 명령어 역시 당연히 다른 종류의 괄호나 구분자에서도 동작합니다:



```
--> try [ with 'square' brackets ] too!
```

## 12.4 매치 모드 감싸기 사용하기 (USING MATCH MODE SURROUND)

매치 모드를 사용하여 현재 선택 영역의 양옆에 괄호 등의 쌍을 씌울 수도 있습니다(surround). 예를 들어 아래 줄로 이동한 후:

- i) "select all of this" 부분을 선택합니다 (예: 일반 모드에서 select 단어 앞으로 이동 후, `v`를 눌러 선택 모드로 진입하고 `4e`를 눌러 다음 4단어를 선택).
    
- ii) `ms(` 또는 `ms)` 를 눌러 선택 영역 양 끝에 괄호를 씌워 보세요.
    



```
--> so, select all of this, and surround it with ()
```

다른 구분자로도 똑같이 할 수 있습니다: 예를 들어 아래의 WORD 위에서 `ms'`를 누르면 양옆에 `''` (작은따옴표)가 씌워집니다. `""`나 `{}`, `[]`를 추가하는 것도 시도해 보세요.



```
--> surround this WORD !
```

## 12.5 매치 모드 감싸기 삭제 사용하기 (USING MATCH MODE DELETE SURROUND)

`md` 명령을 사용하여 텍스트를 감싸고 있는 괄호 쌍을 삭제할 수 있습니다. 아래 줄에서 `()` 쌍 내부 아무 곳으로 커서를 이동하세요 (예: 'x' 위). 일반 모드 상태에서 `md(` 또는 `md)` 를 누르면 감싸고 있던 괄호 쌍이 지워집니다.



```
--> delete (the x pair of parentheses) from within!
```

자연스럽게 다른 종류의 둘러싼 기호들도 지울 수 있습니다:



```
--> delete (nested [delimiters]): "this" will delete the nearest
matching surrounding pair.
--> delete "layers "of" quote marks" too: this will delete the
nearest previous and following quote marks
```

존재하지 않는 구분자 쌍을 삭제하려고 하면 하단 상태 바에 오류가 표시되며 아무 일도 일어나지 않습니다.

## 12.6 매치 모드 감싸기 교체 사용하기 (USING MATCH MODE REPLACE SURROUND)

`mr` 명령을 사용하여 텍스트를 감싸고 있는 기호 쌍을 다른 기호 쌍으로 교체할 수 있습니다. 아래 줄에서 `()` 쌍 내부 아무 곳으로 이동한 후 (예: 'x' 위치), 일반 모드에서 `mr([` 를 누르면 `()` 쌍이 `[]` 쌍으로 바뀝니다.



```
--> replace the (pair from x within), with something else
```

이 명령은 커서를 감싸는 가장 가까운 쌍에 작용하므로, 다음 문장들에서 여러 가지 교체 연습을 해보세요:



```
--> some (nested surroundings [can be replaced])
--> this "works with 'other surroundings' too"
```

존재하지 않는 쌍을 교체하려고 시도해 보세요: 하단 바에 오류 경고가 표시되고 아무 동작도 하지 않습니다.

## 12장 요약 (CHAPTER 12 RECAP)

`m` 키를 눌러 매치 모드로 들어갈 수 있습니다. 팝업에서 사용 가능한 동작들이 표시됩니다. 이 모드를 통해 다음 작업들을 할 수 있습니다:

- `mm`을 입력하여 짝을 이루는 기호(괄호 등) 반대편으로 점프합니다 (커서가 기호 위에 있어야 합니다).
    
- `mi(` 와 같은 명령으로 커서를 둘러싼 괄호 내부만 선택합니다 (기호 자체는 제외).
    
- `ma(` 와 같은 명령으로 커서를 둘러싼 괄호 쌍과 내부를 모두 선택합니다.
    
- `md(` 와 같은 명령으로 둘러싸고 있는 괄호 쌍을 삭제합니다.
    
- `ms(` 를 사용하여 현재 선택 영역 양옆을 괄호 기호로 감쌉니다.
    
- `mr([` 와 같은 명령으로 선택 영역을 감싸고 있는 `()` 쌍을 `[]` 쌍으로 교체합니다.
    

## 13.1 새 분할 창 만들기 (CREATE NEW SPLIT)

일반 모드에서 `Ctrl-w`를 누르면 창(Window) 메뉴가 열리고 사용 가능한 명령어 목록이 표시됩니다.

현재 창의 오른쪽 절반에 수직 분할(vertical split)로 새로운 빈 버퍼를 열려면 `Ctrl-w nv`를 사용하세요 (즉, `Ctrl`과 `w`를 동시에 누른 다음, 손을 떼고 `n`, `v`를 차례로 누릅니다). 현재 창이 수직으로 두 개로 분할될 것입니다. 새로운 빈 버퍼가 오른쪽 절반에 나타나며 커서가 그곳으로 자동 이동합니다.

수평 분할(horizontal split)로 새로운 빈 버퍼를 만들려면 `Ctrl-w ns`를 누르세요. 이 작업은 현재 창을 수평으로 둘로 나누고 새 버퍼를 만들며 커서를 새로운 수평 창으로 이동시킵니다.

## 13.2 분할 창 간 이동하기 (MOVE BETWEEN SPLITS)

`Ctrl-w k`를 사용하면 현재 창 위에 있는 분할 창으로 이동합니다. `Ctrl-w j`를 누르면 아래쪽 분할 창으로 이동합니다. `Ctrl-w h`는 왼쪽 창으로, `Ctrl-w l`은 오른쪽 창으로 이동시킵니다. 분할 창이 열린 순서대로 다음 창으로 계속 넘어가려면 `Ctrl-w w`를 누르세요.

이제 새로운 버퍼와 분할 창에서 원하는 작업을 할 수 있습니다. 해당 버퍼 창을 다 쓰고 나면 `Ctrl-w q`를 눌러 창을 닫을 수 있습니다. `Ctrl-w l`을 누른 후 `Ctrl-w j`를 눌러 우측 하단의 창으로 이동한 다음, `Ctrl-w q`를 눌러 해당 분할 창만 닫아 보세요.

현재 활성화된 창을 제외한 모든 분할 창을 한 번에 닫으려면 `Ctrl-w o`를 누르면 됩니다. `Ctrl-w nv`로 세 번째 수직 창을 열고, `Ctrl-w h`를 두 번 눌러 맨 왼쪽 창으로 이동한 다음, 이 상태에서 `Ctrl-w o`를 눌러 이 창을 제외한 나머지 창을 전부 닫아 보세요.

## 13.3 현재 버퍼 분할하기 (SPLIT CURRENT BUFFER)

`Ctrl-w s`를 사용하여 현재 보고 있는 버퍼를 그대로 수평 분할하고, `Ctrl-w v`를 사용하여 수직으로 분할합니다. 동일한 버퍼 내용이 양쪽 분할 창에 모두 표시될 것입니다.

다시 단일 창 보기로 돌아가려면 `Ctrl-w o`를 눌러 여분의 분할 창들을 닫으세요.

## 13.4 명령어를 사용하여 분할하기 (USE COMMANDS TO SPLIT)

명령 모드인 `:vsplit` (짧게 `:vs`) 및 `:hsplit` (짧게 `:hs`) 명령을 사용하여 특정 버퍼를 수직 또는 수평으로 분할할 수도 있습니다. 예를 들어 다음 명령을 입력해 보세요:

`:vs something`

오른쪽에 "something"이라는 이름의 새 수직 창이 열립니다. 여기서 "something"은 기존에 존재하는 파일이 아니므로 해당 이름을 가진 새 버퍼가 열립니다; 하지만 "something" 대신 실제 파일 이름을 입력하면 새 창에 해당 파일이 열리게 됩니다. 마찬가지로 다음 명령을 입력할 수 있습니다:

`:hs some_more`

아래쪽 창에 "some_more"라는 이름의 새 버퍼가 열립니다. "some_more" 자리에 열고 싶은 파일이나 경로를 넣으면 빈 버퍼 대신 특정 파일을 바로 열 수 있습니다.

## 13.5 분할 창 교환하기 (SWAPPING SPLITS)

`:vs hello1` 명령으로 왼쪽에 분할 창을 열고, `:hs hello2` 명령으로 아래쪽에 창을 하나 더 여세요.

hello2 창에 있는 상태에서 `Ctrl-w K`를 누르면 위쪽에 있는 창과 위치를 교환합니다. 이제 hello2가 맨 위로 올라가고 hello1이 아래로 내려갑니다.

여전히 hello2에 있는 상태에서 이번엔 `Ctrl-w H`를 누르면 왼쪽 창과 위치를 바꿉니다: 이제 hello2가 왼쪽에, 튜토리얼 창이 우측 상단에 배치됩니다. `Ctrl-w` 뒤에 대문자 `HJKL`을 사용하면 각각 왼쪽 / 아래쪽 / 위쪽 / 오른쪽의 버퍼 창과 자리를 바꿀 수 있습니다.

다시 튜토리얼 창으로 돌아온 뒤, `Ctrl-w o`를 눌러 이 창 하나만 남겨 두세요.

## 13.6 분할 창 위치 바꾸기 (TRANSPOSE SPLITS)

`:vs hello1`을 입력하여 수직 창을 열고, `:hs hello2`를 입력해 수평 창을 하나 여세요.

튜토리얼 창으로 커서를 옮긴 뒤 `Ctrl-w t`를 눌러 이 창에서 파생된 분할 구도를 위치 반전(transpose) 시킵니다: 이제 hello1과 hello2가 튜토리얼 창의 우측이 아닌 '아래쪽'에 배치될 것입니다. `Ctrl-w t`를 한 번 더 누르면 원래 구도로 되돌아갑니다.

hello1 창으로 이동한 뒤 `Ctrl-w t`를 누르면 이번엔 여기서 열렸던 수평 분할 구조가 반전됩니다: 이제 hello2가 hello1의 '아래쪽'이 아닌 '오른쪽'에 위치합니다. `Ctrl-w t`를 눌러 원래 상태로 돌려놓으세요.

다시 튜토리얼 창으로 돌아와 `Ctrl-w o`를 눌러 튜토리얼 창을 제외한 모든 창을 닫으세요.

## 13.7 파일 피커에서 분할 창 열기 (OPEN SPLIT FROM FILEPICKER)

파일 피커(file picker) 화면에서 직접 분할 창으로 파일을 열 수도 있습니다. `space f`를 눌러 파일 피커를 엽니다. 거기서 텍스트를 입력해 퍼지 매칭(fuzzy matching) 검색을 할 수 있고, 위아래 화살표를 사용해 선택한 파일(`>` 기호로 표시됨)을 변경할 수 있습니다. 파일 피커를 종료하려면 `Escape`를 누르세요.

파일 피커에서 원하는 파일을 아무거나 선택하세요. 여기서 Enter를 누르면 현재 보고 있는 창에 그 파일이 열리게 됩니다 (지금은 누르지 마세요). 하지만 새 분할 창으로 해당 파일을 열 수도 있습니다. `Ctrl-v`를 누르면 선택한 파일이 새로운 수직 분할 창으로 열립니다. 다시 `space f`를 누르고 파일을 고른 뒤, 이번엔 `Ctrl-s`를 눌러 수평 분할 창으로 열어 보세요.

튜토리얼 창으로 다시 돌아와서 `Ctrl-w o`를 눌러 이 창을 뺀 나머지 창을 모두 닫으세요.

## 13장 요약 (CHAPTER 13 RECAP)

분할 창(Splits)을 사용하면 같은 버퍼를 여러 개의 창에 동시에 띄우거나, 서로 다른 여러 버퍼를 볼 수 있습니다. 메인 창 및 분할 관련 명령어에 접근하려면 `Ctrl-w`를 누르세요. `Ctrl-w hjkl`을 통해 분할 창 사이를 이동할 수 있고, `Ctrl-w q`로 창을 닫을 수 있으며, `Ctrl-w o`를 사용해 현재 보고 있는 창만 남기고 전부 닫을 수 있습니다.

또한 `:vs FILENAME` 과 `:hs FILENAME` 명령어를 사용해서 창을 분할하며 열 수도 있습니다.

파일 피커 메뉴 내에서도 즉시 분할 창으로 열기가 가능합니다. 파일을 선택한 상태에서 `Ctrl-v`를 누르면 새로운 수직 분할 창에 열리고, `Ctrl-s`를 누르면 수평 분할 창에 열립니다.

이 튜토리얼은 현재 계속 작성 중인 상태입니다(work-in-progress). 앞으로 더 많은 내용이 추가될 예정입니다.
